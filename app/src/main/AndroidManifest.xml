<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/qb_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/qb_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuickBuy"
        tools:targetApi="31">
        <activity
            android:name=".category.AllCategoriesActivity"
            android:exported="false" />
        <activity
            android:name=".category.CategoryDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.ProductDetails"
            android:exported="false" />
        <activity
            android:name=".WelcomeActivity"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="false" />
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.MainActivity"
            android:exported="false" />
    </application>

</manifest>